# stuff that needs doing
# priority (LOW,HIGH) and description

LOW readd the C4838 (https://learn.microsoft.com/en-us/cpp/error-messages/compiler-warnings/compiler-warning-level-1-c4838) warning to CMakeLists.txt once they're all fixed in the code
HIGH continue the staged runtime-i18n migration in docs/RUNTIME_I18N_ARCHITECTURE.md: the mandatory 8-language/4-quadrant Text ABI schema and 58-guard campaign/build separation are complete, the first five one-entry Laptop titles use a validated immutable TextPack, the AIM Links title is the second complete pack domain, the Help-screen exit label is the third, the game-clock day label is the fourth, the indexed game-time tables are the fifth, the AIM Sort table is the sixth, and the 14-table/85-entry exporter-only cohort is the seventh; the complete 238-section GameStrings source manifest and linked selected-catalog export adapter are committed, the exhaustive 210-section x 8-language x 4-quadrant raw catalog-source gate proves all 6,720 ranges safe, normal builds static-assert the strict 77-row symbolic-bound contract against live providers, the build-free gate validates all 83,040 selected pointer entries, exact repaired-slot goldens cover the 19 earlier and 104 universal repairs, all 32 ordered output payloads are pinned, and zero exporter-only legacy tables remain. Move the remaining 457 base and 35 JA25 definitions domain by domain, retire the quarantined ENGLISH/GERMAN/etc definitions, and build i18n once. Language changes remain restart-only; voice and hotloading are separate work.
HIGH continue the campaign-policy tail after CampaignQuestPolicy, the typed tactical/strategic sector-content slices, CampaignStrategicAiScenarioPolicy removed the 19 external built-in/custom origin reads, the map-bottom policy retired its four raw availability/exit selectors, CampaignStrategicEventPolicy now owns the complete 14-route dispatcher table, and CampaignApplicationPolicy now gates both UB-options bootstrap calls; preserve left-hand capability gates and keep state/effects in their established owners while migrating the remaining strategic and tactical content domains.
